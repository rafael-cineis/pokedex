/*
 * FavoritePokemonList Messages
 *
 * This contains all the text for the FavoritePokemonList container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.FavoritePokemonList'

export default defineMessages({
  favoritePokemons: {
    id: `${scope}.favoritePokemons`,
    defaultMessage: 'Pokémons Favoritos',
  },
})
